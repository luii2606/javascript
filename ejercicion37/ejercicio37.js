// 37. Un camión transporta 25 cajas de repuestos de carros. Si cada caja pesa 748 Kg ¿cuántos
// Kg transporta?

function carga() {
  let cajas = 25;
  
  let total = cajas * 748;

  alert(`el camión transporta ${total} kg`);
}
carga();